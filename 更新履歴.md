# WebCAD 更新履歴

## バージョン4.7: 2026年9月22日 表示・操作の設定 🔧
オプションに「表示・操作」欄を追加し、見やすさ・操作しやすさを端末ごとに調整できるようにしました。
**何も変えなければ、今までと同じ見た目・動作です。**

### 1. 変えられる項目
| 項目 | 選択肢 | 効くところ |
|---|---|---|
| ルーペの大きさ | 小・**中**・大・特大 | 指でなぞるときの拡大鏡 |
| ルーペの倍率 | 2倍・**3倍**・4倍・6倍 | 同上 |
| 座標の文字 | 小・**中**・大・特大 | ステータスバー・全画面の座標・ルーペの座標 |
| 座標の桁 | **標準**・1・0.1・0.01・0.001 | 同上（標準＝ステータスバーは整数、ルーペは小数2桁） |
| 寸法の文字 | 小・**中**・大・特大 | 寸法の文字と矢印、基点測定の文字 |
| 吸着の範囲 | 狭い・**標準**・広い・最大 | スナップが点に吸い付く範囲 |

（太字が初期値。「表示・操作を初期値に戻す」でまとめて戻せます）

- ルーペと吸着の範囲は、切り替えた直後に**見本を1.6秒表示**します。オプション画面に隠れるときは、その間だけ画面を透かします。
- 設定はこの端末（ブラウザ）に保存され、次に開いたときも同じです。

### 2. あわせて直したところ
- ルーペの下の座標は、スナップしているときは**スナップ点（確定で入力される点）の座標**を出すようにしました（以前はスナップ前の指の位置でした）。
- スマホのステータスバーは、座標が長いとき（桁を増やしたとき・文字を大きくしたとき）に **X と Y を2段に折り返す**ようにしました。狭い画面では、変わることのない「モデル」表示を隠して座標に場所を譲ります（もともと右端が数pxはみ出していたのも直りました）。
- 寸法・測定の操作ボタン（確定・記入など）を、ステータスバーの座標と重ならない高さに出すようにしました。
- オプションなどのパネルが画面より長いときは、中身をスクロールできるようにしました。

## バージョン4.6: 2026年9月20日 ファイル構成の整理 🧱
見た目と操作は変わりません。中身（プログラムの置き場所）を整理して、今後の追加・修正を安全にするための更新です。

### 1. 大きくなった1ファイルを役割ごとに分けました
- 4200行あった `cad-core.js` を、次の7つに分けました（処理内容はそのまま移しただけです）。
  - `cad-view.js` 座標変換・UCS・ズーム
  - `cad-geom.js` スナップ・タップ判定・空間索引
  - `cad-render.js` 画面描画
  - `cad-command.js` コマンド処理
  - `cad-input.js` マウス・タッチ・キーボード
  - `cad-panels.js` ブロック管理・画層管理・プロパティ
  - `cad-boot.js` 起動処理（一番最後に読み込む）
- どこに何があるか分かりやすくなり、不具合の原因を探す範囲が狭くなります。

### 2. 壊れたことに気づける仕組みを追加
- スクリプトの読み込み順、名前の重複、1ファイルの大きさを自動テストで確認します（読み込み順が崩れると画面が出なくなるため）。
- 未定義の名前を見つける設定を自動化し、ファイルが増えても登録漏れが起きないようにしました。

### 3. 使われていないコードの削除
- どこからも呼ばれていない対応表（コマンド名とツール名）と、存在しない関数の呼び出し1か所を削除しました。

## バージョン4.5: 2026年9月20日 基点測定とパネルの移動 📐
図面上の1点を基点にして、そこからの距離を見ながら作業できるようになりました。

### 1. 基点測定（基点からの寸法を出したまま見る）
- ツールバーの **📐 測定**（またはコマンド `MEASURE` / `MEA`、ドロワーの「計測」→「基点測定」）で始めます。
- 基点をなぞって「☑️確定」すると、そこから指（カーソル）やスナップ点までの
  **X距離（北）・Y距離（東）・直線距離** を画面に出したままにします。
- 指を離した位置に表示が残るので、そのまま読み取れます。画面を拡大・移動しても基点は図面上の同じ場所に残ります。
- 「📐 記入」を押すと、いま見えている測定値を図面の寸法として記入します（X方向の平行寸法・Y方向の平行寸法・直線の整列寸法の3つを1つのまとまりとして）。
- 「📍 基点」を押すと、いま測っている点を次の基点にします。続けて測るときに使います。
- 図面の1単位＝1m の設定なら m で小数3桁、1mm の設定なら mm の整数で表示します（オプションの「測量座標の単位」に従います）。
- UCS を回転している場合、X・Y は図面本来の軸（WCS）で測ります（開始時にお知らせします）。

### 2. フローティングパネルを動かせるようにしました
- 座標一覧・オプション・画層一括管理などのパネルは、**見出しをつまんで好きな場所へ移動**できます。図面の見たい場所が隠れなくなります。
- パネル全体が必ず画面の中に収まるように位置を制限します（画面外に出て見失いません）。
- 置いた場所は次に開いたときも覚えています。見出しを**ダブルタップすると元の位置（画面中央）に戻ります**。

### 3. あわせて直したところ
- 寸法線の位置をちょうど 0（測った2点を直接結ぶ位置）にしたとき、既定値が使われて離れた場所に描かれていたのを直しました。
- 指を動かさずにタップしたとき、前に合わせたスナップ点が残ったままになることがあったのを直しました。
- 画面下のアクションバーは、ボタンが増えても文字が縦に折り返されないようにしました。

## バージョン4.4: 2026年9月12日 測量機能（SIMA・座標一覧・現在地） 📍
測量の現場でよく使うデータのやり取りと、現在地の確認ができるようになりました。

### 1. SIMA・座標CSVの読み込み
- 「開く」から **SIMA（.sim）** と **座標CSV（.csv / .txt）** を読み込めます。文字コード（Shift-JIS / UTF-8）は自動で判定します。
- 測点は「点の記号＋点名」を1つのまとまりとして配置し、画層「測点」「測点名」に入れます。SIMA の区画データ（D00〜D99）は閉じた線（画層「区画」）と区画名になります。
- 座標CSVは「点名,X,Y,標高」または「点番号,点名,X,Y,標高」の並びに対応します（見出し行・タブ区切り・"…" で囲んだ項目も可）。
- X は北、Y は東として扱います（アプリの座標表示と同じ）。

### 2. SIMA・座標CSVの書き出し
- メニュー「測量」から **SIMA出力**（Shift-JIS、区画付き）と **座標CSV出力**（Excel で文字化けしない UTF-8）ができます。
- 対象は、点（SIMA・CSVで取り込んだ点、作図した点）、**属性に点名を持つブロック**（DXF/DWG の測点記号。挿入点を座標とする）、区画です。
- 書き出した SIMA を読み直すと同じ点・区画になることを自動テストで確認しています。

### 3. 座標一覧
- メニュー「測量」→「📍 座標一覧」（または COORDS コマンド）で、図面の測点を一覧できます。点名・点番号で検索でき、行をタップするとその点へ移動して選択します。
- 取り込んだ DXF の測点ブロックも一覧に出ます。ブロックを移動・回転すると一覧の座標も一緒に変わります。

### 4. 現在地（GNSS）
- メニュー「測量」→「🛰 現在地」で、スマホの位置情報を図面上に青い点と精度の円で表示します。緯度・経度を平面直角座標（19系）に換算します（国土地理院の換算式。別の公式とmm単位で一致することを確認）。
- 初回は図面の系番号（I〜XIX系）を選びます。現在地から推定した系も表示します。
- 「中心へ」「追従」「＋点（現在地に点を追加）」ができます。図面から大きく離れている場合は、系番号や単位の設定違いを知らせます。
- 精度はスマホの GPS に依存します（数m程度）。標高は使いません。

### 5. その他
- 点を選ぶと、プロパティで点名・点番号・標高・座標を編集できます。
- 閉じた線（区画など）を選ぶと、面積（㎡）を表示します。
- オプションに「図面の1単位の長さ（1m / 1mm）」と「現在地の座標系（系番号）」を追加しました。

## バージョン4.3: 2026年9月11日 大きな図面の軽量化 ⚡
3万図形規模の現場図面でも、スマホで操作が重くならず、ブラウザが落ちないようにしました。（数値はPCのChromeで3万図形の図面を計測。スマホではおおむね4〜5倍の時間がかかります）

### 1. 「元に戻す」の履歴がメモリを使いすぎる問題を解消
- 以前は編集のたびに全図形を丸ごと複製して履歴に残していたため、20回の編集で約200MB、最大50回分で約500MBを使い、スマホのブラウザが落ちる恐れがありました。
- 図形ごとに記録し、変わっていない図形は前の履歴と共有するようにしました。**20回の編集で約4MB**になりました。
- 編集1回ごとの記録にかかる時間も 57ms → 23ms、元に戻す1回は 69ms → 27ms に短縮しました。

### 2. タップ判定・スナップを高速化（空間索引）
- 図面を格子に区切って図形を登録しておき、指の周りの格子の図形だけを調べるようにしました。
- タップ判定は1回あたり 1.2ms → 0.01ms 未満、作図中のスナップ探索（指を動かすたびに実行）は 2.1ms → 0.13ms になりました。
- 索引は編集・元に戻す・読み込みのたびに自動で作り直します。索引を使った結果が全図形を調べた場合と完全に一致することを自動テストで確認しています。

### 3. 描画の軽量化
- **ルーペ（拡大鏡）**: 指の周りしか映らないのに全図形を毎回描いていたのを、映る範囲だけにしました（拡大表示＋ルーペの1フレーム 18ms → 1.8ms）。
- **全体表示**: 同じ色の線をまとめて描き、画面上で1ピクセルに満たない図形は点に、読めない大きさの文字は線に簡略表示するようにしました（1フレーム 52ms → 22ms）。拡大して見る分には、以前と1画素も変わらないことを確認しています。
- **ピンチ・ホイールでの拡大縮小中**: 重い図面では、直前の画面を拡大縮小して見せ、指を離したら正確に描き直します（地図アプリと同じ方式。操作中の1フレーム 22ms → 6ms）。
- **作図中に指やマウスを動かしたとき**: カーソル・スナップ記号・ルーペだけを描き直し、図形は描き直さないようにしました（1フレーム 18ms → 5ms）。
- 軽い図面では、以前と同じく毎回すべて描きます（見た目は変わりません）。

### 4. その他の不具合修正
- 「延長」で伸ばした線の外形情報が古いままで、伸ばした部分が画面の端で描かれない・タップやスナップに反応しないことがあったのを修正しました。
- 保存データに寸法の画面上の当たり判定（描画のたびに作り直す一時データ）が含まれていたのを除きました。

## バージョン4.2: 2026年9月11日 品質強化（自動テスト・自動チェック） 🧪
アプリの主要な処理に自動テスト（107件）を整え、テストで見つかった不具合を修正しました。GitHub にプッシュするたびに自動でチェックが走ります。

### 1. テストで見つかり修正した不具合
- **DXFの円弧が欠片になる**: DXF読み込みで円弧の角度を二重に変換しており、0°〜90°の円弧が約1.6°の欠片になっていました（円弧は既定で非表示のため気付きにくい状態でした）。正しい形で読み込むようにしました。書き出したDXFを読み直した場合も同様に直ります。
- **トリムで線ごと消える**: 長方形やポリライン（取り込んだ境界線など）と交わる線をトリムすると、交点で切れずに線全体が消えていました。長方形・ポリラインの辺も境界として切れるようにしました。
- **延長が長方形・ポリラインに届かない**: 延長の境界に長方形とポリラインを追加しました。
- **円弧の描かれていない部分まで使っていた**: トリム・延長で円弧を「全円」として扱っていたため、描かれていない部分で切れたり、そこまで伸びたりしていました。描かれている範囲だけを境界にしました。
- トリムで残った線がブロック（グループ）から外れていたのを、元のグループに残すようにしました。オフセットで作った図形は逆に元のブロックに入らないようにしました。
- 長さ0の線をオフセットすると座標が壊れた図形（NaN）ができていたのを修正しました。
- ポリラインを回転すると座標が0.001単位に丸められていた（他の図形は丸めない）のを修正しました。
- **選択アクションバーで1つ選んだ図形を操作できない**: 図形を1つタップして選んでも、バー（や左ツールバー）の「削除・移動・複写・回転」では引き継がれず、もう一度タップが必要でした。そのまま操作できるようにしました。

### 2. 図形の固有ID（選択のずれを根本解決）
- すべての図形に固有IDを付け、選択状態を「何番目の図形か」ではなくIDで保持するようにしました。
- 以前は、長押しで文字を消した・トリムした・元に戻した等で図形の並びが変わると、選択が別の図形にずれていました。たとえば2つ選んだ状態で別の文字を長押し削除し、続けて「削除」すると、選んでいない図形が消えることがありました。
- プロパティ画面の入力欄も、画面を開いたまま図形の並びが変わっても正しい図形を編集します。
- 保存データにもIDが入ります。古い保存データは開いたときに自動でIDを付けます。

### 3. DXF書き出しの改善
- 長方形・ポリラインを、線分の集まりではなく1つのポリライン（LWPOLYLINE）として書き出すようにしました。他のCADで境界線を1図形として扱えます。
- 楕円が書き出されずに消えていたのを修正しました。
- 文字の揃え（中央・右寄せ・上下）を書き出すようにしました（以前は読み直すと文字の位置がずれていました）。
- 図形ごとに付けた色を書き出すようにしました（以前はすべて画層の色になっていました）。色番号表に無い色は、最も近い色番号にします（以前は白になっていました）。

### 4. 自動テストと自動チェック
- 実際のアプリを試験用のブラウザ環境で動かすテストを整備しました（DXF/DWG取り込み、書き出し→読み直し、交点・オフセット・回転、スナップ、トリム/延長、自動保存、固有ID、選択アクションバー）。「npm test」で実行できます。
- GitHub にプッシュすると、構文チェック・自動テスト・本番ビルドが自動で実行されます（GitHub Actions）。失敗した版は、公開される前に GitHub 上で気付けます。

## バージョン4.1: 2026年9月11日 基盤強化（オフライン・データ保護） 🛡
電波の無い現場でも確実に動き、作業中の図面が消えないよう、アプリの土台を強化しました。

### 1. 完全オフライン対応
- **図面の読み書きライブラリをアプリに同梱**: DXF読込（dxf-parser）・DXF書出（dxf-writer）・DWG読込（libredwg-web）を外部サーバー（CDN）から毎回取得していたのをやめ、アプリ本体に同梱しました。一度開いた端末なら、機内モードでも図面を開く・書き出すことができます。
- **DWG読込エンジンの取得量を約16MB→約2.3MBに削減**: 以前は同じエンジンを「WASM埋め込み版」と「WASM単体」の2つとも取得していました。1つに統一し、圧縮転送で約2.3MBになりました。
- **アップデート後もオフラインで使える**: 以前はアプリ更新のたびにライブラリのキャッシュが全削除され、更新直後に電波が無いとDWGが開けませんでした。変更の無いファイルは更新後も保持するようにしました。
- オプション画面の「オフライン・データ」で、DWG読込エンジンの保存状況の確認と「端末に保存」ができます（電波のある場所で1回DWGを開いた場合も自動で保存されます）。
- 電波が弱い場所では、4秒待っても新しい版が届かなければ端末内の版ですぐ起動します（以前は通信が終わるまで真っ白のまま待たされることがありました）。

### 2. アプリ更新の確実化
- デプロイ直後に「新しい画面＋古いプログラム」の組み合わせで動いてしまう問題を解消しました（ファイル名に内容のハッシュを付けて配信）。
- 起動中に新しい版が届くと「新しいバージョンに更新されました［再読み込み］」を表示します。再読み込み前に作業中の図面を保存し、再起動後は確認なしでその図面に戻ります。
- キャッシュの版番号をビルド時に自動で付けるようにし、上げ忘れで古い版が残る事故を防ぎます。オプション画面の最下部でビルド番号を確認できます。

### 3. 作業データの保護（自動保存の強化）
- **自動保存の間隔を「5分ごと」から「変更の15秒後（編集し続けても最長60秒）」に短縮**しました。
- **アプリを裏に回した瞬間にも保存**します。スマホではタブを閉じる時の保存処理がほぼ動かず、以前は最大5分の作業が消えることがありました。
- 元に戻す/やり直し、図面を開いた直後も自動保存の対象にしました。
- 自動保存から復元したとき、プロジェクト名・図面名も元に戻るようにしました。
- 保存容量が足りない等で保存に失敗した場合、見逃さないよう画面に通知します（以前は失敗しても「成功」扱いになることがありました）。
- ブラウザの容量整理で保存データが自動削除されにくくなるよう「永続ストレージ」を要求します。オプション画面で保存領域の状態を確認できます。
- プロジェクト一覧から別の図面を開くとき、名前を付けて保存していない変更がある場合だけ正しく警告するようにしました（以前は「現在の作業は自動保存されます」と表示されていましたが、実際には上書きされていました）。

### 4. エラーの見える化
- 想定外のエラーが起きると、画面下に「⚠ エラーが発生しました」を表示します。スマホでは開発者ツールが使えず、黙って動かなくなる状態の原因が分からなかったためです。
- 「詳細」から直近30件のエラー記録を確認でき、「コピー（不具合報告用）」で端末情報つきで貼り付けられます。オプション画面の「エラーログ」または ERRORS コマンドでも開けます。
- DXF/DWGの読み込みに失敗した場合や、DWGから図形を読み取れなかった場合も、画面に理由を表示します。

### 5. その他
- アプリアイコンの実寸が宣言（512×512）と違い（2974×3020・約5MB）、インストール時に5MBを転送していたのを、正しいサイズで約140KBにしました。
- 使われていなかった重複ファイル（ルートの古い sw.js・アイコン・manifest）を削除しました。

## バージョン4: 2026年9月11日 アップデート 🧩
図面取り込み（DXF/DWG）の精度を大幅に改善し、ブロック・グループをスマホでまとめて扱えるようにしました。

### 1. 図面取り込みの不具合修正（DXF）
- **ブロック基点のずれを修正**: ブロック定義の基点（BLOCK 10/20）が考慮されておらず、基点が(0,0)でない記号ブロックが挿入点からずれて配置されていた問題を解決しました。
- **入れ子ブロックの回転・縮尺**: ブロック内にさらにブロックがある場合、親の回転と縮尺が子に引き継がれていなかった問題を修正しました。鏡像（負の縮尺）の円弧も正しい向きになります。
- **画層の色が全部白になる不具合**: dxf-parser の画層情報は「色番号(colorIndex)」と「RGB整数(color)」の2種類があり、RGB整数を色番号として読んでいたため色が付きませんでした。色番号を使うよう修正し、元図面で非表示/フリーズだった画層は非表示のまま取り込みます。
- **属性文字（ATTRIB）に対応**: 測点名・番号などブロックの属性値が読み込まれていませんでした（dxf-parser未対応）。独自ハンドラを追加し、不可視属性は除外、定数属性はブロック定義の値を表示します。
- **寸法（DIMENSION）の実形状を展開**: 寸法線・矢印・寸法値が入った匿名ブロック（*Dnn）を展開して表示します（従来は点と文字だけ）。
- **円弧付きポリライン（bulge）**: 道路などの曲線ポリラインが直線の弦になっていたのを、円弧を折れ線で近似して忠実に再現します。
- **スプライン**: 制御点を結ぶだけだったスプラインを、ノットを使って曲線として評価します。
- **文字の位置合わせ・回転**: 中央/右寄せ文字の位置合わせ点（code 11）を使うようにし、TEXT/MTEXT/ATTRIBの回転角を取り込んで回転描画します。
- **SOLID（塗り矢印など）の頂点順**を修正し、蝶ネクタイ状に崩れていた形を直しました。
- 画層テーブルに無い画層名を持つ図形は、その場で画層を作成します（従来は画層0へ）。ペーパー空間のブロック参照を除外。
- 読み込み結果を **トースト表示**（読み込み個数・未対応個数）し、未対応の種類はコマンドログに内訳を出します。
- **開くときに「置き換え」か「現在の図面に追加」かを選べる**ようにしました（従来は常に追加＝重なっていた）。置き換え時は保存プロジェクト名も解除され、前の名前で上書き保存される事故を防ぎます。
- 重い画層の自動非表示のしきい値を大きく引き上げました（従来は200個以上の画層が開いた瞬間に消え、ログが畳まれているスマホでは原因が分からなかった）。
- DWG読み込みでも、既存画層と重複がある場合に画層の割り当てがずれる不具合を修正し、ブロックのグループ化・属性・文字回転に対応しました。

### 2. ブロック・グループ管理（スマホ向け）
- 取り込んだブロック（INSERT 1つ＝1グループ）と寸法は自動でグループになり、**図形をタップするとブロック全体が選択**されます（オプションでOFF可）。
- 選択中は画面下に **選択アクションバー** が出て、移動・複写・回転・削除・隠す・グループ化/分解・ズームをその場で実行できます。
- **ブロック管理パネル**（上部バーの 🧩、全画面ドロワー、メニュー、「BLOCKS」コマンド）: ブロック名ごとの一覧から、種類単位の表示/非表示・ズーム・一括選択ができます。
- 任意の複数図形を **GROUP**（G）でグループ化、**UNGROUP/EXPLODE**（X）で分解できます。複写したグループは別グループになります。
- 削除・移動・複写・回転はグループ単位で動作し、Undoで一括で戻せます。

### 3. その他の不具合修正・改善
- 画面端に差しかかった文字が消える（文字の境界がアンカー点のみだった）問題を修正。文字は本体の矩形でタップ選択できるようになりました。
- 中ボタンドラッグ（PC）でUCS原点が動いてしまう不具合を修正（画面パンに）。
- 空白をタップしても複数選択が残り、次のERASEで消えてしまう問題を修正。
- ボタンの振動フィードバックが二重に登録されていたのを一本化。
- 「図面を閉じる」で画層・プロジェクト名もリセット。DXF出力のファイル名に図面名を使用し、文字の回転角も出力。
- オプションに「タップでブロック全体を選択」「取り込み時に円弧を非表示にする」を追加。
- 画層の表示切替・画層非表示もUndo対象に。回帰テストを「node tests/test-text-parse.cjs」に移行（ESM設定との競合を解消）。

## バージョン3: 2026年7月3日 アップデート 🎉
システム監査に基づく不具合修正と基盤の安定化を行いました。オフセット等の編集後にスナップが効かなくなる不具合、およびテキストの文字化けを根本解決し、アプリ更新が即座に届くようService Workerを刷新しました。

### 1. 編集コマンド後にオブジェクトスナップが効かなくなる不具合の解決
- **OFFSET（オフセット）で作成した図形がOスナップの対象にならない**不具合を修正しました。原因は、図形の当たり判定に使う境界ボックス（bbox）が編集後も古い位置のまま残り、スナップ点の探索から除外されてしまうことでした。
- 同じ原因を持つ **MOVE（移動）・COPY（コピー）・ROTATE（回転）後のスナップ不良・描画消え** の潜在バグも一括で修正しました。

### 2. テキスト文字化けの完全解決（3つの原因すべてに対応）
- **文字コードの自動判定**: DXF読み込みがShift-JIS固定だったため、新しいAutoCAD（2007形式以降）のUTF-8なDXFで日本語が化けていました。UTF-8を先に試し、だめならShift-JISで読む自動判定を導入しました。
- **MTEXT書式除去の書き直し**: 本文にセミコロン（;）が含まれると文字が消える貪欲マッチの問題を、MTEXTの仕様に沿った順序処理へ書き直して解決。過去のデグレーション実例を含む**回帰テスト31件**（`node tests/test-text-parse.cjs`）を整備し、再発を防止します。
- **Unicodeエスケープ・特殊記号の変換**: 古いDXFの `\U+XXXX` 形式の日本語や、`%%c`（φ）`%%d`（°）`%%p`（±）が未変換のまま表示される問題を解決しました。
- MTEXTの改行（`\P`）が複数行として正しく描画されるようになりました。

### 3. Service Workerの刷新（アプリ更新の即時反映とオフライン強化）
- 従来はデプロイしてもキャッシュ優先のため更新がユーザーに届きにくい構造でした。HTMLはネットワーク優先・アセットは表示しつつ裏で更新（stale-while-revalidate）に変更し、**デプロイが次回起動時に確実に反映**されます。
- DWG読み込みに必要な libredwg-web（WASM）をキャッシュ対象に追加し、**オフラインでのDWG読み込み**に対応しました。

### 4. 座標読取モード: XYトグルOFFで座標ツールチップを完全非表示に
- 全画面の座標読取モードで、右下の **XY** トグルボタンをOFFにすると、タッチ位置に追従する座標ツールチップが**一切表示されなくなりました**。寸法計測などでツールチップが邪魔になる場合にOFFにしてください（従来はOFFでも触っている間は表示されていました）。

### 5. セキュリティ・保守性の改善
- 保存プロジェクト一覧の画面組み立てを安全なDOM API方式に変更（プロジェクト名に記号を含む場合の表示崩れ・HTML混入を解消）。
- 機密性の観点から実案件の図面ファイルをリポジトリの全履歴から完全除去しました。
- モックアップ類を `docs/mockups/` へ、テスト用データを `tests/fixtures/` へ整理しました。

## バージョン: 2026年5月31日 アップデート
他者から受け取ったごちゃごちゃした図面を素早く綺麗に整理し、現場での作業効率を劇的に高めるための「画層一括管理・うっすらフェード表示機能」や「タッチ非表示ツール」、および日本語フォントを含む「DXF/DWGテキストインポート不具合の完全解決」を行いました。

### 1. ドラッグ可能な「画層一括管理」フローティングパネル
- 画面表示領域を狭めていた従来の右側サイドパネルを廃止し、ドラッグ可能なフローティングパネル内でスマートに画層管理（個別切り替え、全表示、全非表示、表示反転）を行えるようにアップグレードしました。

### 2. 表示・非表示リストのダイナミック分離管理
- パネル内で画層を **「👁️ 表示中」** と **「➖ 非表示中」** の2グループに完全に分けて表示します。
- 👁️ / ➖ ボタンをクリックすると、項目が対応するグループへスムーズに自動移動します。
- それぞれの見出しの右側に **[すべて非表示]** および **[すべて表示]** ボタンを新設し、ワンタップで一括操作が可能になりました。
- 作図対象（カレント画層）は **📌** マークと太字で強調され、リスト内の画層名をタップするだけで安全にカレント画層を切り替えられます。

### 3. 連続タッチ非表示モード（LAYOFFコマンド）
- 図面上の図形（線や円、寸法など）をタッチ（クリック）するだけで、そのオブジェクトと同じ画層を即座に非表示にできる **「タッチ非表示ツール」** を追加しました。
- 終了するまで連続して画面上の不要な要素をタップして消していけるため、ごちゃごちゃした図面を非常に素早く綺麗に整理できます（タッチ成功時には30msの心地よい微振動が返ります）。

### 4. スマートフォン・タブレット向けの終了UI対応
- タッチ非表示モード起動時、画面下部に赤い **「✖ 非表示終了」** アクションバー（フローティングボタン）が自動表示され、Escキーや右クリックがないスマホ環境でも安全にワンタップで終了できるようにしました。
- フローティングパネル内の「👆 タッチ非表示中」ボタンをもう一度タップすることでも、トグル動作として安全に終了できます。

### 5. 非表示画層の「うっすら表示（フェード表示）」機能
- フローティングパネル最上部に **「非表示画層をうっすら表示する」** チェックボックスを新設しました。
- ONにすると、非表示に設定された画層の図形や寸法が、不透明度 15% の薄い半透明で画面上に残るようになります。他の図形との位置関係や参照を確認しながら邪魔な要素を整理するのに極めて便利です。

### 6. 交差選択（範囲選択）のトグルスイッチ化（デフォルトOFFによる誤動作防止）
- モバイルでのピンチズームやドラッグ（移動）時に、意図せず範囲選択（矩形選択）が始まって誤作動してしまう問題を完全に解消しました。
- 画面下のステータスバーおよび全画面HUDに **「範囲選択」** トグルボタンを追加し、**初期状態を「OFF」** に設定することで、意図的な操作時のみONにして範囲選択ができるようガードを施しました。

### 7. DXF/DWG テキスト・マルチテキスト（MTEXT）読み込み処理の一時的な復元（5/30時点）
- **動作の安定性を考慮した一時復元**: 5月31日に追加した日本語フォントや多重フォールバックの一連の修正により、特定の図面においてテキスト読み込み自体が失敗する予期せぬ挙動が発生したため、まずは安定性を優先し、テキストパース処理を **5月30日時点の元の安定したコード（コミット `72d4cf6`）へ完全にロールバック（復元）** しました。
- 昨日以前に正常に読み込みできていた図面テキストは、すべて元通り正常にインポート・表示されます。

### 8. 全画面HUDの「範囲選択」ボタン削除および並列寸法値の重なり解消
- **全画面HUDの整理**: 全画面モード時に画面右下に配置されるコントロールパネル（HUD）から、「範囲選択」ボタンを削除しました。これにより、誤タップを防ぎつつUIがクリーンに整理されました。
- **並列寸法値の重なり解消**: 連続寸法コマンドで並列（PARALLEL）寸法を作図する際、寸法線同士の段差間隔（PARALLEL_STEP）を `10` から `25`（ピクセル）へと広げました。これにより、文字サイズ `12px` の寸法値テキストや寸法線同士が上下に重なってしまう不具合が完全に解消され、極めて高い視認性と美しい段状のレイアウトを両立しました。

## バージョン: 2026年5月
本バージョンでは、図面の自動保存機能の追加やiOS環境での全画面表示対応など、現場作業での安心感と使い勝手を向上させるアップデートを行いました。

### 1. プロジェクトの自動保存・読込機能
- 描画中の図面データがブラウザ内に自動保存されるようになりました。
- 万が一ブラウザを閉じてしまったりリロードした場合でも、次回起動時に自動で直前の状態が復元されます。
- 保存済みの図面一覧から、いつでも過去のプロジェクトを呼び出せるようになりました。

### 2. iOS (Safari) での全画面表示対応
- フルスクリーンAPIに制限があるiPhone等のSafariブラウザにおいても、「疑似フルスクリーン」モードにより画面を広く使えるように改善しました。

### 3. 寸法入力の操作性向上
- 寸法を入力する際、画面下部に専用の「寸法入力アクションバー」が表示されるようになりました。「確定」「直列モード切り替え」「終了」などの操作が迷わず行えます。

### 4. タッチ操作のフィードバック強化
- メニューやボタンをタップした際に、スマートフォン等で軽い触覚フィードバック（バイブレーション）が返るようになり、確実な操作感が得られるようになりました。

### 5. 全画面モード・UIのレイアウト改善
- 全画面時のツールバー（メニューボタン群）を画面右側に縦一列で配置するように変更し、操作性と視認性を向上させました。
- 全画面モード専用の「画層（レイヤー）切り替えドロップダウン」を新設し、モバイル環境でも作図レイヤーを自由に変更できるようになりました。
- 非全画面時にズームボタン等が誤って表示されてしまう不具合を解消しました。

### 6. UCS2点 (UCS2P) 指定の操作性向上
- 座標寸法などと同様に、画面タップ時に「プレビュー表示（黄色のマーカーや線）」を行い、アクションバーの「☑️ 確定」ボタンを押すことで本決定される確実な操作フローへ変更しました。
- 1点目（原点）指定時は近接点へのスナップを無効化し、2点目（方向）指定時に近接点スナップを復活させるよう、オンスナップの挙動を最適化しました。

### 7. 長押し削除の安全対策
- 長押し（0.6秒）による削除機能を「自身で配置した寸法オブジェクト（直列寸法や座標寸法など）」のみに制限し、図面本体（線分など）を誤って消してしまう事故を防止しました。
- 全画面モード時だけでなく、PC等の通常モードでも長押し（または右クリックと同等のタッチ操作）による削除が可能になりました。

### 8. DXF読込時の円弧（ARC）非表示対応
- DXFファイル等を読み込む際、円弧（ARC）オブジェクトに対して自動的に非表示設定を行うようにしました。これにより、不要な円弧で画面がごちゃつくのを防ぎます。

### 9. 連続寸法の方向固定機能の追加
- 連続寸法コマンド実行時、アクションバーに「⚙ 自動 (X/Y)」切替ボタンを追加しました。
- このボタンを押すことで、連続寸法の作図方向を強制的に「水平（X軸）」または「垂直（Y軸）」に固定できるようになりました。

### 10. PC環境における操作性向上 (UCS2点)
- PC（マウス操作）環境にて、UCS2点 (UCS2P) コマンドを実行する際、モバイル向けの「プレビュー確認・確定ボタン」ステップをスキップし、2点目をクリックした瞬間に確定・完結するように改修しました。
- 2点目の方向指定時、画面上のラバーバンド（黄色のガイド線）が近接点等のオンスナップ座標へ正確に追従（吸着）するように改善しました。

## バージョン: 2026年4月
本バージョンでは主に測量・現場作業に向けた機能の強化、使い勝手の向上、およびバグフィックスを行いました。

### 1. 座標読取モード（全画面表示）の強化
- **フローティングメニューの追加**: 画面上部に半透明の専用ツールバーを追加し、UCS操作（UCS、UCS2点、保存、WCS）、座標寸法（DIMORDINATE）、PLAN機能などがスピーディに行えるようになりました。
- **レイアウトの調整**: 誤タップを防ぐため、座標ツールチップを上部ツールバーと被らない位置（左上から少し下げた位置）へ自動退避させるように改善しました。

### 2. UCS（任意座標系）機能の大幅拡張
- **UCS2点 (UCS2P) 機能**: 画面上の任意の2点をタップするだけで、その線を「測量X軸（進行方向）」として即座に原点・軸角度を設定できるようになりました。
- **保存・読込機能**: 設定した自身のオリジナル座標系（UCS）に名前を付けて保存し、ドロップダウンからいつでも簡単に切り替え・呼び出しができるようになりました（全画面モードからも「💾保存」で直接保存可能）。

### 3. PLAN機能（画面の水平回転）の追加
- **斜めの図面を水平に**: 斜めに設定したUCSであっても、「PLAN」ボタンを直角・水平に画面が自動回転し、正面視点で作業できるようになりました（解除も可）。
- 回転状態でも正確にオンスナップや寸法配置などの座標読み取り・描画が行えるよう、計算エンジンを完全対応させました。

### 4. 測量特化の「座標寸法 (DIMORDINATE)」デザイン刷新
- 1度のタップと引出しで、1つの引出線の上下に「X: 測量Y（北方向）」「Y: 測量X（東方向）」が2列でまとめて出力される専用スタイルへ全面改修しました。外の強い日差し下でも視認しやすい緑系の配色を採用しています。

### 5. タッチ操作・UXの向上
- **長押しでの即座削除**: エンティティ（図形や寸法）を0.6秒間押し続けるだけで、選択操作を挟まずに即座に削除（バイブレーション付き）できるようになりました。
- **クロスヘア（十字カーソル）の色**: 長時間見つめても目が疲れにくい「薄いキャメル色（#e2c288）」へ変更しました。
- ピンチズーム時に画面が「ぐるぐる」回ったり、ズーム中心がズレていくバグを根本的に解消し、OSNAPポイントや指の中心でのズームがより滑らかで正確になりました。
