# WebCAD 操作マニュアル

このドキュメントは、スマートデバイスやPCの両方で利用できるWebブラウザベースの2D CADアプリケーション **「Antigravity Web CAD」** の操作説明書です。

## 🌐 アクセスURL
お使いのスマートフォンやPCのブラウザ（Chrome, Safari等）から以下にアクセスしてください。特別なインストールは不要です。
**[https://antigravity-web-cad.vercel.app](https://antigravity-web-cad.vercel.app)**

---

## 📱 基本操作（画面の移動・拡大など）

### PCでの操作
* **画面の移動（パン）**: マウスの「ホイール（中ボタン）」を押したままドラッグ。
* **画面の拡大・縮小（ズーム）**: マウスのホイールを前後に回転。
* **図形の選択**: 図形を左クリック（複数選択可）。何も選択していない状態でドラッグすると「範囲選択」になります。
  * **左から右へドラッグ（窓選択・青枠）**: 枠の中に完全に収まっている図形のみが選択されます。
  * **右から左へドラッグ（交差選択・緑枠）**: 枠に少しでも触れている図形がすべて選択されます。

### スマートフォン（タブレット）での操作
* **画面の移動（パン）**: 2本指で画面に触れ、そのままスライドします。
* **画面の拡大・縮小（ズーム）**: 2本指でつまんだり広げたりします（ピンチイン・ピンチアウト）。
* **図形の選択**: 図形付近を1回短くタップします。
* **正確な位置の指定（ルーペ機能）**: 画面を1本指で長押ししながらスライドすると、指の上に拡大鏡（ルーペ）が現れます。指を離した場所が決定ポイントになります。
* **範囲選択**: 1本指でスライドさせることで範囲選択の枠が描画されます。（※「全画面モード」中は無効になります）

---

## 🔲 全画面表示（座標読取モード）

スマートフォン等での現場作業において、図面上の座標値を正確に読み取ることに特化したモードです。画面上部の「🔲 全画面」ボタンを押すと切り替わります。

* **専用UI表示**: ツールバーやコマンドラインが非表示になり、図面を広く確認できます。誤作動防止のためドラッグによる「範囲選択」は無効化されます。
* **フローティング座標ツールチップ**: 1本指で画面をなぞると、指の右上に現在のX・Y座標値（小数点以下切り捨て）とスナップ状態がリアルタイムで表示されます。
* **XY 常時表示ボタン (右下)**: ONにすると、指を離しても画面左上に常に座標ツールチップが表示されたままになり、値をメモする際に便利です。
* **OSNAP ボタン (右下: ⊕)**: 全画面モード中もスナップ機能のON/OFFを手軽に切り替えられます。
* **ズームスライダー (左下)**: 縦方向のスライダーおよび「＋」「−」ボタンで直感的なズームが可能です。スナップ（端点など）を捕捉している状態で操作すると、**捕捉しているポイントを中心にしてズーム**するため、正確にスナップできているかの確認が簡単に行えます。
* **全体表示 (右下: ⛶)**: 図面全体が画面に収まるようにズームをリセットします。
* **解除 (右下: ✕ または 左上: ✖ 解除)**: 全画面モードを終了し、通常の編集画面に戻ります。

---

## 🛠 コマンドの使い方

新しい図形を描いたり、編集したりするには、画面左側のツールバーにあるアイコンをタップするか、画面下部のコマンドラインに文字を入力してコマンドを起動します。

### 📌 描画コマンド
| コマンド | エイリアス | 概要 |
| :--- | :--- | :--- |
| **LINE** | `L` | 線分を描画します。（1点目と2点目を指定） |
| **CIRCLE** | `C` | 円を描画します。（中心点と半径を指定） |
| **RECTANG** | `REC` | 長方形を描画します。（対角線を指定） |
| **ARC** | `A` | 円弧を描画します。（3点・中心などを指定） |
| **PLINE** | `PL` | 連続した線（ポリライン）を描画します。「完了」ボタンで確定します。 |
| **ELLIPSE** | `EL` | 楕円を描画します。（中心・長半径・短半径を指定） |
| **TEXT** | `T` | 文字を入力します。（プロパティから後で書き換え可能） |
| **HATCH** | `H` | 選択した閉じた図形を半透明で塗りつぶします。 |
| **POINT** | `PO` | 測量座標などで利用する「点」を描画します。 |

### ✏️ 編集コマンド
| コマンド | エイリアス | 概要 |
| :--- | :--- | :--- |
| **ERASE** | `E` | 図形を削除します。あらかじめ選択してから実行してもOKです。 |
| **MOVE** | `M` | 図形を別の場所へ移動します。（基点と目的点を指定） |
| **COPY** | `CO` | 図形をコピーします。（基点と目的点を指定） |
| **OFFSET** | `O` | 任意の距離を指定して、図形を平行に複写します。 |
| **ROTATE** | `RO` | 図形を回転させます。（基点と参照角度を指定可能） |

### 📐 寸法・表示・測量支援
| コマンド | エイリアス | 概要 |
| :--- | :--- | :--- |
| **DIM** | (DLI, DAL等) | 平行寸法、角度寸法、直径・半径寸法、座標寸法などを引き出します。 |
| **UNDO/REDO** | `U` / `REDO` | 直前の操作を元に戻す、またはやり直します。 |
| **ZOOM** | `ZE` | 全体表示。図面全体が画面に収まるよう表示位置を調整します。 |
| **UCS** / **WCS** | - | 任意の場所に原点を移動（UCS）、または本来の原点にリセット（WCS）します。 |

---

## 🗺 測量仕様に最適化された機能

本アプリは、測量図面や現場での利用に特化した設計となっています。

1. **X座標・Y座標の入れ替え表示**
   * 一般的な数学的CADとは異なり、システム内の座標は「X: 測量座標系のX（画面の縦軸）」「Y: 測量座標系のY（画面の横軸）」として直感的に表示されます。
   * スマホ上の常時座標表示も `X: 12345  Y: 12345` のような並びで小数点なし表示となり、現場での読み取りに最適化されています。

2. **UCS（任意座標系）の活用**
   * `UCS` コマンドで原点を現場の基準点などに移動させると、そこからの相対的な座標（X,Y距離）をプロパティやツールチップで簡単に確認できます。

---

## ⚙ その他の機能

* **OSNAP（オブジェクトスナップ）機能**
  * 画面下部の `OSNAP` ボタンでON/OFFを切り替えます。
  * `OSNAP`ボタンの横の「▼」を押すと設定パネルが開き、「端点」「中点」「交点」「中心」「近接点」「垂線」など、吸着させたい種類を細かく指定できます。ルーペ内にもスナップポイントが大きくハイライトされます。
* **DXF / DWGファイルの読み込み・保存**
  * **読込**: 画面左上の「📁 開く」ボタンを押し、お手元の **.dxf** または **.dwg** ファイルを選択して読み込みます。
  * **保存**: 画面上部の「DXF」「DWG」ボタンを押すと、現在の状態をファイルとして書き出して保存（ダウンロード）できます。
* **画層（レイヤー）とプロパティの編集**
  * **画層パネル（上部 👁️ アイコン）**: 図面のレイヤーごとに「表示/非表示」を切り替えたり、色を変更したりできます。
  * **プロパティパネル（上部 📋 プロパティ）**: 図形を選択してパネルを開くと、**サイズ変更**、**レイヤー移動**、**色の変更**、**座標の直接入力編集**などが可能です。
