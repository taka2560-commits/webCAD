# WebCAD 操作マニュアル

このドキュメントは、スマートデバイスやPCの両方で利用できるWebブラウザベースの2D CADアプリケーション **「Antigravity Web CAD」** の操作説明書です。

**対応バージョン: v4.9（2026年9月22日）** — 詳細は[更新履歴.md](./更新履歴.md)をご参照ください。

## 🌐 アクセスURL
お使いのスマートフォンやPCのブラウザ（Chrome, Safari等）から以下にアクセスしてください。特別なインストールは不要です。
**[https://antigravity-web-cad.vercel.app](https://antigravity-web-cad.vercel.app)**

---

## 🔰 はじめての方へ（操作ガイド）

アプリの中に、操作しながら覚えられるガイドがあります。

* **はじめてツアー（約3分）**: 初めて開いたときに「はじめての方へ」が出ます。「ツアーを始める」を押すと、練習用の図面で「拡大・縮小 → 画面の移動 → 全体表示 → 図形を選ぶ → ルーペと座標 → 測る → 元に戻す → 保存」を順に体験できます。書いてあるとおりに操作すると次へ進みます（押してほしいボタンや点が緑の枠で光ります）。開いていた図面はツアーのあとで元に戻り、練習用の図面は保存されません。
* **座標読取モードのツアー**: 座標読取モードの入り方、なぞって座標を読む、座標を残す、拡大縮小、スナップ、道具箱、戻り方を体験できます。
* **操作中のヒント**: コマンドを始めると、画面上部に「いまやること」が出て、操作が進むと次の手順に変わります（各コマンドの最初の3回。「常に」「出さない」も選べます）。
* **つまずいたときの一言**: 1本指で画面を動かそうとしたとき（→2本指で）、寸法で ☑確定 を押していないときなどに、画面下に一言だけ助けが出ます。
* **ヘルプ（❓）**: 右下の **？**、⋯ メニューの「❓ ヘルプ・操作ガイド」、座標読取モードでは右下の **？** から開きます。機能ごとの説明と「▶ やってみる」（その機能だけのミニツアー）、コマンド一覧、ヒントの設定があります。

---

## 📱 基本操作（画面の移動・拡大など）

### PCでの操作
* **画面の移動（パン）**: マウスの「ホイール（中ボタン）」を押したままドラッグ。
* **画面の拡大・縮小（ズーム）**: マウスのホイールを前後に回転。
* **図形の選択**: 図形を左クリック（複数選択可）。何も選択していない状態でドラッグすると「範囲選択」になります。
  * **左から右へドラッグ（窓選択・青枠）**: 枠の中に完全に収まっている図形のみが選択されます。
  * **右から左へドラッグ（交差選択・緑枠）**: 枠に少しでも触れている図形がすべて選択されます。

### スマートフォン（タブレット）での操作
* **画面の移動（パン）**: 2本指で画面に触れ、そのままスライドします。
* **画面の拡大・縮小（ズーム）**: 2本指でつまんだり広げたりします（ピンチイン・ピンチアウト）。
* **図形の選択**: 図形付近を1回短くタップします。
* **文字・寸法の即座の削除（長押し）**: 通常画面で何もしていないときに、文字や寸法を「0.6秒間押しっぱなし」にすると、操作メニューを挟まずに即座に削除されます（完了時にバイブと「↩ で元に戻せます」の表示でお知らせ）。座標読取モード・コマンドの途中・2本指の操作中は消えません。
* **正確な位置の指定（ルーペ機能）**: 画面を1本指でなぞる（ドラッグする）と、指の少し上に拡大鏡（ルーペ）が現れます。クロスヘア（十字カーソル）を見ながら正確なスナップ位置を探し、指を離した場所が決定ポイントになります。
* **範囲選択**: 「全画面モード」ではない状態で1本指でスライドさせることで、範囲選択の枠が描画されます。

---

## 🔲 全画面表示（座標読取モード）

スマートフォン等での現場作業において、図面上の座標値を正確に読み取ることに特化したモードです。画面上部の「🎯 座標読取モード」ボタンを押すと切り替わります。

* **専用UI表示**: サイドツールバーやコマンドラインが非表示になり、図面を広く確認できます。誤作動防止のためドラッグによる「範囲選択」は無効化されます。
* **フローティング上部ツールバー**: 全画面モードの上部には半透明のメニューが常時表示されます。ここから「📍座標（寸法）」「🎯UCS」「📈2点（UCS）」「💾保存（UCS）」「↩WCS」「🧭PLAN」「UCS切り替えドロップダウン」に直接アクセスできます。
* **フローティング座標ツールチップ**: 1本指で画面をなぞると、指の左上などに現在の測量X・Y座標値とスナップ状態がリアルタイムで表示されます。
* **XY 表示ボタン (右下)**: 座標ツールチップの表示そのものをON/OFFします。ONの状態では指でなぞると座標が表示され、指を離すと画面左上に常に座標ツールチップが残り、値をメモする際に便利です。**OFFにするとツールチップは一切表示されなくなり**、寸法計測などでツールチップが視界の邪魔になる場合に活用できます。
* **OSNAP ボタン (右下: ⊕)**: スナップ機能のON/OFFを手軽に切り替えられます。
* **ズームスライダー (左下)**: 縦方向のスライダーおよび「＋」「−」ボタンで直感的なズームが可能です。スナップ（端点など）を捕捉している場合は、**捕捉しているポイントを中心にしてズーム**します。

---

## 📲 新モバイルUI（v3標準）

スマートフォンでの操作性を高めるため、上部バー・左ツールバー・コマンドラインがコンパクトなデザインに統一されています。

* **左ツールバー（アイコンレール）**: 通常は44px幅のアイコンのみのレール表示です。上部の `☰` をタップするとラベル付きの一覧パネルへ展開し、`◀` または `☰` の再タップで折りたたまれます。
* **コマンドライン（ピル表示）**: 画面下部は通常「`● READY · コマンド待機中`」のような32pxの細いピル（帯）表示です。タップすると全ログと入力欄が展開し、入力行右の `▼` ボタンで元のピル表示に戻せます。
* **⚙ オプション（背景色）**: 画面上部の「⚙ オプション」から、キャンバスの背景色を **黒／グレー／白** に切り替えられます。設定は次回起動時も保持されます。白背景を選んだ場合、グリッドや十字線・白系の図形色は自動的に見やすい暗色へ補正されます。
* **コマンド別のフローティング数値入力パネル**: `CIRCLE`（半径）・`RECTANG`（幅×高さ）・`OFFSET`（距離）・`ROTATE`（角度）を実行すると、画面上に数値を直接入力できる小さなパネルが表示されます。数値を入力して確定すると、その後は1回のタップ／クリックだけで図形を配置できます。空欄のまま進めると、従来通り2点のクリックで作図する方式になります。
* **配色の役割固定**: 青＝座標系・ORTHO関連、緑＝確定・保存・OSNAP、水色＝寸法、黄＝編集、赤＝削除、として色の意味を統一し、現場での視認性を高めています。

---

## 🛠 コマンドの使い方

新しい図形を描いたり、編集したりするには、画面左側のツールバーにあるアイコンをタップするか、画面下部のコマンドラインに文字を入力してコマンドを起動します。

* **座標で点を入れる**: 点を指定する場面で、コマンドラインに `X,Y` の順（X＝北・Y＝東。画面の座標表示と同じ）で入力します。例: `100,200` → X（北）100・Y（東）200。`@5,-3` のように `@` を付けると、直前の点から北へ5・西へ3 の点になります。
* **キーボード（PC）**: Esc でコマンドをやめます（ポリラインは描いた部分を残す）。何もしていないときの Esc は選択の解除です。Ctrl+Z（⌘Z）で元に戻す、Ctrl+Y（⌘Y・Ctrl+Shift+Z）でやり直し。

### 📌 描画コマンド
| コマンド | エイリアス | 概要 |
| :--- | :--- | :--- |
| **LINE** | `L` | 線分を描画します。（1点目と2点目を指定） |
| **CIRCLE** | `C` | 円を描画します。（中心点と半径を指定） |
| **RECTANG** | `REC` | 長方形を描画します。（対角線を指定） |
| **ARC** | `A` | 円弧を描画します。（3点・中心などを指定） |
| **PLINE** | `PL` | 連続した線（ポリライン）を描画します。画面下の「☑確定」で完了、「⭘閉じる」で閉じた形にします（途中でやめても描いた部分は残ります）。 |
| **ELLIPSE** | `EL` | 楕円を描画します。（中心・長半径・短半径を指定） |
| **TEXT** | `T` | 文字を入力します。（プロパティから後で書き換え可能） |
| **HATCH** | `H` | 選択した閉じた図形を半透明で塗りつぶします。 |
| **POINT** | `PO` | 測量座標などで利用する「点」を描画します。 |

### ✏️ 編集コマンド
| コマンド | エイリアス | 概要 |
| :--- | :--- | :--- |
| **ERASE** | `E` | 図形を削除します。あらかじめ選択してから実行してもOKです。（長押し削除も可能） |
| **MOVE** | `M` | 図形を別の場所へ移動します。（基点と目的点を指定） |
| **COPY** | `CO` | 図形をコピーします。（基点と目的点を指定） |
| **OFFSET** | `O` | 任意の距離を指定して、図形を平行に複写します。（線・円・円弧・長方形・ポリライン。ポリラインの角は延長して結びます） |
| **ROTATE** | `RO` | 図形を回転させます。（基点と参照角度を指定可能。寸法・塗りつぶしも一緒に回ります） |
| **MIRROR** | `MI` | 図形を鏡に映します。対象を選び、鏡の線の1点目・2点目を指定します。設定パネルの「元の図形を残す」で、映した複写を作るか、元の図形を動かすかを選びます。文字は裏返さず、読める向きのまま映します。 |
| **SCALE** | `SC` | 図形の大きさを変えます。対象を選び、基点（動かない点）を指定します。倍率は設定パネル（またはコマンド欄に数）で。「長さで」に今の長さと新しい長さを入れると倍率を求めます。 |
| **ARRAY** | `AR` | 図形を並べます。「縦横に並べる」は横（→）・縦（↑）の数と間隔（UCS の向き。マイナスで左・下）、「円形に並べる」は中心をタップして数と角度（360° なら一周に等間隔）。☑確定 で作ります。 |
| **BREAK** | `BR` | 図形を1点で2つに分けます（交点・端点などに吸い付きます）。閉じた形（閉じたポリライン・長方形）は、その点で開いた1本になります。設定パネルの「等分する」（コマンド `DIV`）では、タップした図形を同じ長さに分けます（円は円弧に）。 |
| **JOIN** | `J` | 線をタップすると、つながった線（分かれ道まで）をまとめて選びます（もう一度タップで外す・範囲選択で足す）。☑確定 で1本のポリラインにします。一周すれば閉じた形（🧮求積に使えます）、一直線なら1本の線、同じ円の上で続く円弧は1つの円弧（一周なら円）になります。すき間 0.5mm まではつながっているとみなします。 |
| **FILLET** | `F` / `CHA` | 角の処理。「丸める」は2本の線の残す側をタップすると、半径の円弧でつなぎます（半径 0 は、線を延ばす・切って角を出します）。「面取り（隅切り）」は角から両側の長さ、または底辺（切る線）の長さで角を切ります。ポリライン・長方形の角は、角をタップするだけで面取りできます。 |

* **点を動かす（グリップ）**: 図形をタップして選ぶと、端点・頂点・中点・中心などに青い四角（グリップ）が出ます。四角に指を置いたまま動かす先までなぞって離すか、四角をタップしてつかみ（緑）、動かす先をタップすると、その点が動きます（スナップ・ルーペが使えます。PC はクリック、または押したまま動かして離す）。
  * 線の端・中点（線ごと）、ポリラインの頂点（辺の中の薄い四角は点を足す）、長方形の角・辺、円の中心・半径、円弧の両端・中点（3点を通る円弧に）・中心、楕円、文字の位置、寸法の測った点・寸法線の位置・半径寸法の向きを動かせます。
  * 動かしたあとも図形は選んだままなので、続けてほかの点も動かせます。やめるときは ❌終了 か Esc。四角の上から始めなければ、なぞって座標を読んでも図形は動きません。

### 📐 寸法・表示・機能
| コマンド | 概要 |
| :--- | :--- |
| **寸法 (DIM)** | 平行寸法、角度寸法、直径・半径寸法などを引き出します。 |
| **連続寸法 (DIMCONT)** | 選択した基準寸法から連続して寸法（直列・並列）を作図します。作図中のアクションバーで「⚙ 自動 (X/Y)」ボタンを押すと、寸法の向きを水平（X軸）または垂直（Y軸）に固定して引き出すことができます。 |
| **座標寸法 (DIMORDINATE)** | 「📍座標」ボタン。ワンタップで指定箇所のX/Y両方の測量座標を引出線付きで配置します。 |
| **基点測定 (MEASURE)** | 「📐測定」ボタン。基点からのX距離・Y距離・直線距離を出したまま確認し、「📐記入」で寸法として残せます。 |
| **UNDO/REDO** | 直前の操作を元に戻す、またはやり直します。 |
| **ZOOM (ZE)** | 🔍 全体。図面全体が画面に収まるよう表示位置を調整します。 |
| **PLAN** | 🧭 PLAN。設定したUCSの角度に合わせて、画面を自動回転させて「水平・直角（正面視点）」にします。 |

---

## 🗺 測量仕様に最適化された機能

本アプリは、測量図面や現場での利用に特化した設計となっています。

1. **X座標・Y座標の測量表示対応**
   * システム内の座標は「X: 測量座標系のX（北方向・数学的Y）」「Y: 測量座標系のY（東方向・数学的X）」として直感的に表示されます。
   * 座標の桁と文字の大きさは、オプションの「表示・操作」で変えられます（標準はステータスバーが整数、ルーペが小数2桁）。
   * コマンドラインで座標を入れるときも同じ順（`X,Y` ＝ 北,東）です。

2. **UCS（任意座標系）のフル活用**
   * **🎯 UCS**: 任意の場所に原点を移動し、そこからの相対的な座標（X,Y距離）を作成します。
   * **📈 UCS2点**: 画面上の2点を指定するだけで、その線を「測量X軸（進行方向）」とする新しい座標系を即座に作成します。現場の通り芯を基準にする際に便利です。（PC環境では、2点目を画面クリックした時点で即座に設定が完了します。また2点目指定時のガイド線は近接点等へのスナップに正確に追従します。）
   * **💾 保存**: 設定したUCSを名前をつけて保存し、いつでも切り替え・呼び出しが可能です。
   * **🧭 PLAN**: 斜めになった現場の通り芯（設定したUCS）を図面上に水平に表示し直すことで、現場の向きと画面の向きを一致させることができます。

3. **🧮 測量計算（v5.0）** — ⋯ メニューの「🧮 測量計算」（コマンド: `COGO`・`AREA`・`INV`）
   * **点の指定**: 各欄の 📍 を押し、図面の点までなぞって ☑確定（PC はクリック）。測点に吸い付き、点名もそのまま使います。欄に点名・点番号・`X,Y`（北,東）を入れてもかまいません。
   * **求積**: 「👆 求積する区画をタップ」で区画（閉じたポリライン・長方形・SIMA の区画）の線か内側をタップすると、座標法の求積表（倍面積・面積・地積・周長）を出します。地積の端数（0.01㎡未満／1㎡未満の切り捨て）を選べます。
     * 📋 求積表を図面に置く／📏 辺長を記入／㎡ 面積を記入／📄 CSV出力。
   * **逆計算**: 2点の ΔX・ΔY・水平距離・方向角（度 分 秒）。「終点から次の点へ ▶」で続けて計算できます。
   * **点の追加**: 座標で／方向角と距離で／後視点と夾角（後視を 0° とした右回りの角）で、測点を追加します。点名は続き番号を提案します。
   * **交点**: 2直線（延長線上も）／方向角×2／距離×2（A→B の右側・左側を選ぶ）の交点を、測点として追加します。
   * 方向角・夾角は「45.5」（度）または「45 30 15」（度 分 秒）で入れます。計算は図面の座標（WCS）で行い、距離・面積は m・㎡ です（図面の単位が mm でも同じ）。

4. **📡 トータルステーション連携（v5.1）** — ⋯ メニューの「📡 TS連携」（コマンド: `TS`）。ソキアの SDR 形式（SDR33・SDR2x）でやり取りします。
   * **ファイルで受け取る**: 機械の現場データ（.sdr）を 📁開く で開くと、器械点・後視・目標高・観測から座標と標高を計算して測点にします。
   * **ファイルで渡す**: 「📄 SDR33 で書き出す」（座標一覧の「SDR33出力」、コマンド `SDROUT` でも）。機械で既知点・杭打ち点として読み込みます。日本語などを含む点名は P0001 などに置き換えます。
   * **通信で受け取る**（PC の Chrome・Edge、Android の Chrome）: 「🔌 機械とつなぐ」→ 機械で「データ → 現場 → 通信出力 → S type（SDR33）」。届いた点を図面に重ねて表示するので、確かめて「✅ 図面に取り込む」。
   * **通信で渡す**: 機械を「既知点 → 通信入力 → S type」にしてから「📤 測点を送る」（範囲選択した測点、無ければすべて）。
   * Bluetooth は先に端末の設定でペアリングします。ケーブルのときは通信設定（ボーレートなど）を機械と同じにします。iPhone・iPad はファイルで受け渡しします。

5. **📍 杭打ちナビ（v5.2）** — ⋯ メニューの「📍 杭打ち」（コマンド: `STAKE`・`KUI`）
   * **杭の順番**: 範囲選択した測点（無ければすべて）を ◀ ▶ で選びます。「✓ 済」で済みにして、次の済んでいない杭へ進みます（もう一度押すと取り消し）。🔍 で杭を画面の中央に出します。杭は点名・`X,Y` の入力や 📍 でも指定できます。
   * **現在地から**: 「🛰 現在地を使う」で、スマホの現在地から杭までの距離・向き（矢印）・北へ／東へ を大きく表示します。🧭「向きに合わせる」で、矢印がスマホの向きを基準にします（磁北の目安）。杭の近く（現在地の精度の範囲）に入ると振動で知らせます。GNSS は数 m ずれるので、杭のおおよその場所を探すときに使います。
   * **杭打ちの記録**: 「✓ 済」を押すと、現在地と杭の差（実測 − 計画の ΔX・ΔY・距離）・現在地の精度・日時をその測点に記録します（器械点から案内しているときは日時だけ）。記録は図面と一緒に保存され、↩ で戻せます。済んだ杭でもう一度押すと記録を消します。「📄 杭打ち記録 CSV」で書き出せます。
   * **器械点から**: 器械点・後視点を指定すると、杭の夾角（後視を 0° とした右回り）・水平距離・方向角・高低差を出し、順番の杭をまとめた表を出します。「📋 杭打ち表を図面に置く」「📄 CSV出力」ができます。

---

## ⚙ その他の機能

* **OSNAP（オブジェクトスナップ）機能**
  * 画面下部の `OSNAP` ボタンでON/OFFを切り替えます。
  * `OSNAP`ボタンの横の「▼」を押すと設定パネルが開き、吸着させたい種類を選べます: 端点・中点・中心・交点・垂線・接線・挿入点・四半円点・図心・等分点・延長・近接点（設定は端末に保存）。
  * **延長**: 線の上で指を少し止めると、その線を延ばした先（延長・延長交点・延長線上の垂線）にも吸着します。
  * **次の1点だけ**: パネルで種類を押すと、次の1点だけその種類に吸着します（OSNAP ボタンが黄色で「垂線のみ」のように表示）。
  * **候補の切り替え**: 候補が重なったら Tab キー、または寸法・測定の画面下の ⇄ で切り替えます。
  * **2点の中点・相対入力**: パネルの下のボタンから、2点の真ん中や、スナップした点から「北へ・東へ」「距離・方向角」で点を入力できます。
* **表示・操作の設定**: オプションの「表示・操作」で、ルーペの大きさ・倍率、座標の文字と桁、寸法の文字と桁、吸着の範囲を変えられます（寸法の桁「自動」は小数3桁まで・末尾の0は省く）。
* **パネルの移動**: 座標一覧やオプションなどのパネルは、見出しをつまんで動かせます（ダブルタップで中央に戻る）。
* **測量データ**: 「📁 開く」から SIMA（.sim）・座標CSV も読み込めます。⋯ メニューの 📍座標一覧（検索・タップで移動）、SIMA 出力・座標CSV 出力、🛰現在地（GNSS）が使えます。
* **📷 写真・メモ（v5.5）**: ⋯ メニューの「📷 写真・メモ」（コマンド: `PHOTO`・`MEMO`）。
  * 「📍 ピンを立てる」→ 図面の場所までなぞって ☑確定（測点に吸い付き、点名を覚えます）。編集画面でメモを書き、「📷 撮る」（カメラ）・「🖼 選ぶ」（写真の中から、複数可）で写真を付けます。写真は縮めて端末に保存されます。
  * 図面のピン（橙色のしずく形・番号つき）をタップしても開きます。一覧からタップすると、そのピンを画面の中央に出して開きます。ピンは画層「写真・メモ」で隠せます。
  * ピンは図面と一緒に保存され、↩ で戻せます。「🗑 ピンを消す」、写真の ✕ で1枚ずつ外せます。
  * 「📄 写真台帳 PDF」で、写真・番号・場所（点名）・座標・日時・メモを A4 縦に3件ずつまとめた PDF を作ります。写真が複数のピンは 1-1・1-2 のように写真ごとに並びます。印刷・PDF で作る図面にも、同じ番号がピンの位置に入ります。
* **🗺 地図・下絵（v5.4）**: ⋯ メニューの「🗺 地図・下絵」（コマンド: `MAP`）。
  * **地図**: 「標準」「淡色」「写真」を選ぶと、国土地理院の地図を図面の下に重ねます。図面の座標を平面直角座標として重ねるため、系番号を選んでおきます（🛰現在地と同じ設定）。「濃さ」で地図の濃さを変えます（出典：地理院タイル）。一度見た範囲の地図は端末に保存され、電波の無い所でも表示されます（アプリとして開いているとき。「保存した地図」の「消す」で空にできます）。
  * **下絵**: 「📁 画像・PDF を読み込む」で、地積測量図・公図のスキャン（画像・PDF）や写真を図面の下に表示します。PDF はページが複数なら何ページ目かを聞き、そのページを画像にします（はじめて PDF を開くときは、表示エンジンの読み込みに通信があります。オプションの「DWG・PDF の読込エンジンを端末に保存」で先に保存もできます）。「📍 2点で合わせる」を押し、画像の上の点1 → 図面の点1 → 画像の点2 → 図面の点2 の順に指定すると、画像が図面に重なります（画像の上の点は吸い付かず、図面の点は測点に吸い付きます）。離れた2点を選ぶと正確です。
  * 下絵は端末に保存され、「🗑 下絵を消す」まで残ります。「👁 隠す」「画面に合わせて置き直す」もあります。
* **🖨 印刷・PDF（v5.3）**: ⋯ メニューの「🖨 印刷・PDF」（コマンド: `PRINT`・`PDF`）。
  * 用紙（A4〜A1・横／縦）・縮尺（1/100〜1/5000。「画面に合わせる」でいまの画面が入る縮尺）・色（白黒／カラー）・図面名・作成者を決めます。
  * 図面に用紙の枠（黄色の点線）が重なります。画面を動かす・拡大して、枠の中に印刷したい所を入れます（画面の中央が用紙の中央。PLAN で回した向きもそのまま）。右下の四角は表題欄、右上の丸は方位記号の場所です。
  * 「📄 PDF を保存」で PDF ファイル、「🖨 開いて印刷」で新しいタブに PDF を開きます。印刷するときは「実際のサイズ（100%）」を選びます（「用紙に合わせる」にすると縮尺が変わります）。
  * 白黒は線・文字をすべて黒で、カラーは白い紙で読めるように明るい色を濃くして出します。塗りつぶしは薄く（半透明）出します。
* **DXF / DWGファイルの読み込み・保存**
  * **読込**: 画面左上の「📁 開く」ボタンを押し、お手元の **.dxf** または **.dwg** ファイルを選択して読み込みます。DXF の単位（m／mm）が設定と違うときはお知らせします。
  * **書き出し**: ⋯ メニューの「DXF 出力」で、現在の状態を DXF ファイルとして書き出して保存（ダウンロード）できます。寸法・塗りつぶしも含み、単位は設定（m／mm）どおりです。DWG 形式での保存には対応していません（DWG が必要なときは、DXF を CAD ソフトで開いて保存し直してください）。
* **画層（レイヤー）とプロパティの編集**
  * 図形を選択すると、画面右側に「プロパティ」パネルが表示され、色、画層、座標、半径などの数値を直接編集できます。
  * 画面上部の「👁️」ボタンで画層パネルを開き、各画層の表示/非表示の切り替えや新規作成が行えます。

---

## 🔄 更新履歴

最新の更新履歴については、[更新履歴.md](./更新履歴.md) をご参照ください。

**直近のアップデート**
* **2026年9月（v5.7.1）**: TS連携のファイル欄に SIMA の読み込み・書き出し、機械から届いたデータをそのまま見る「🔍 受信した生データ」。
* **2026年9月（v5.7）**: 作図・編集の強化（鏡像・尺度変更・配列・分割／等分・結合・角の処理（フィレット・面取り）、点を動かす（グリップ））。
* **2026年9月（v5.6）**: 杭打ちの記録（現在地との差）、見た地図のオフライン保存、PDF の下絵。
* **2026年9月（v5.5）**: 現場写真・メモ（ピン・メモ・写真、写真台帳 PDF）。
* **2026年9月（v5.4）**: 背景地図（国土地理院の地図を図面の下に重ねる）・下絵（画像を2点で合わせる）。
* **2026年9月（v5.3）**: 縮尺印刷・PDF（用紙・縮尺・図枠・表題欄・方位記号・縮尺バーつきのベクター PDF）。
* **2026年9月（v5.2）**: 杭打ちナビ（現在地からの距離・向き、器械点からの夾角・杭打ち表）。
* **2026年9月（v5.1）**: トータルステーション連携（ソキア SDR33・SDR2x のファイルの読み込み・書き出し、USB・Bluetooth での受信・送信）。
* **2026年9月（v5.0）**: 測量計算（座標法の求積表・地積、逆計算、方向角と距離・後視点と夾角からの点の追加、交点計算）。
* **2026年9月（v4.1〜v4.9.1）**: 完全オフライン対応・自動保存の強化、大きな図面の軽量化、SIMA・座標CSV・座標一覧・現在地（GNSS）、基点測定、フローティングパネルの移動、表示・操作の設定、スナップの追加（延長・接線・挿入点など）と点の指定の補助、はじめての方向けの操作ガイド、点検で見つかった不具合の修正（座標の入力順を X,Y に統一、DXF 出力の寸法・単位・塗りつぶし、画面回転時の表示など）。
* **2026年7月（v3）**: 新モバイルUIの標準搭載、オブジェクトスナップ不具合（オフセット・移動・コピー・回転後）の修正、DXF/DWGテキスト文字化けの解消（文字コード自動判定・MTEXT書式処理の刷新）、アプリを開くだけで最新版に更新される自動アップデート対応、座標読取モードのXY表示ボタンでツールチップを完全非表示にできる機能の追加。
* **2026年5月**: 図面の自動保存・読込機能の追加、iOS(Safari)での疑似フルスクリーン対応、寸法入力専用アクションバーの追加、ボタンタップ時の触覚フィードバックの追加など。
* **2026年4月**: 座標読取モード（全画面表示）の強化、UCS機能の大幅拡張、PLAN機能の追加、座標寸法(DIMORDINATE)の測量仕様への刷新など。
