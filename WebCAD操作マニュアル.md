# WebCAD 操作マニュアル

このドキュメントは、スマートデバイスやPCの両方で利用できるWebブラウザベースの2D CADアプリケーション **「Antigravity Web CAD」** の操作説明書です。

## 🌐 アクセスURL
お使いのスマートフォンやPCのブラウザ（Chrome, Safari等）から以下にアクセスしてください。特別なインストールは不要です。
**[https://antigravity-web-cad.vercel.app](https://antigravity-web-cad.vercel.app)**

---

## 📱 基本操作（画面の移動・拡大など）

### PCでの操作
* **画面の移動（パン）**: マウスの「ホイール（中ボタン）」を押したままドラッグ。
* **画面の拡大・縮小（ズーム）**: マウスのホイールを前後に回転。
* **図形の選択**: 図形を左クリック（複数選択可）。何も選択していない状態でドラッグすると「範囲選択」になります。
  * **左から右へドラッグ（窓選択・青枠）**: 枠の中に完全に収まっている図形のみが選択されます。
  * **右から左へドラッグ（交差選択・緑枠）**: 枠に少しでも触れている図形がすべて選択されます。

### スマートフォン（タブレット）での操作
* **画面の移動（パン）**: 2本指で画面に触れ、そのままスライドします。
* **画面の拡大・縮小（ズーム）**: 2本指でつまんだり広げたりします（ピンチイン・ピンチアウト）。
* **図形の選択**: 図形付近を1回短くタップします。
* **図形の即座の削除（長押し）**: 画面上の図形や寸法を「0.6秒間押しっぱなし」にすると、操作メニューを挟まずに即座に削除されます（完了時にバイブでお知らせ）。
* **正確な位置の指定（ルーペ機能）**: 画面を1本指でなぞる（ドラッグする）と、指の少し上に拡大鏡（ルーペ）が現れます。クロスヘア（十字カーソル）を見ながら正確なスナップ位置を探し、指を離した場所が決定ポイントになります。
* **範囲選択**: スマホ画面上部の「🔲 範囲選択」トグルボタンをON（緑色表示）に設定した状態で、1本指でドラッグすることで範囲選択の枠が描画されます（誤操作防止のためデフォルトはOFFになっています）。

---

## 📱 スマホ最適化ハイブリッドUI（スマホ用メインUI）

スマートフォンなどの画面幅（430px以下）のデバイスでアクセスした際、自動的に適用される片手操作に最適化したプレミアムなUIです（全画面表示モードに移行しなくても、通常の画面幅のまま動作します）。

* **スワイプ可能なヘッダー（上部）**:
  - 画面最上部に、主要な12個の操作ボタンが横並びで配置されます。左右にスワイプ（スクロール）して素早くアクセスできます。
  - ボタン項目: `🎯 座標読取` / `⛶ 全画面` / `🌐 WCS戻す` / `UCS読込選択` / `🧭 PLAN` / `⊕ OSNAP` / `直交` / `🔲 範囲選択` / `📁 開く` / `💾 保存` / `📂 復元` / `🔍 原点` / `✕ 閉じる`
  - 現在の画面回転状態や、OSNAP・直交・範囲選択などのON/OFF状態が色の変化でリアルタイムに確認できます。
* **ボトムバー・ボトムシート（下部）**:
  - 画面最下部には、親指が届きやすいように集約された「ボトムバー」が表示されます：
    - `📁 管理`: 開く、図面保存、復元、PLAN、UCS原点、UCS2点、WCS戻す、全体表示、DXF/DWG出力、新規図面などの管理コマンド。および「キャンバス背景色（黒/グレー/白）」の変更。
    - `➕ 作図`: 線分、ポリライン、長方形、円、円弧、楕円、文字、ハッチの作図コマンド。
    - `↩ 戻す`: 直前の操作を元に戻す（Undo/Redo）。スマホ上での編集誤操作をすばやくリセットできます。
    - `📐 寸法`: 各種寸法（平行、整列、半径、直径、角度、座標、連続寸法）の作図コマンド。
    - `🎨 設定`: 「👁️ 画層一括管理を開く」ボタン（フローティング式の高機能な画層管理ダイアログを開きます）や、簡易的な画層の切り替え、および「端点」「中点」「中心」「交点」「近接点」「垂線」などのOSNAP個別トグルスイッチ。
  - 各項目をタップすると、半透明ガラス風の「ボトムシート」がスッとせり上がり、詳細なサブ機能を選択できます（コマンドを選択すると自動的にシートは閉じます）。
* **コンテキストアクションバー（下部浮動）**:
  - コマンド実行中や、オブジェクト（寸法や図形など）を選択した際には、自動的にボトムメニューが隠れ、状況に応じた「今必要なボタン（確定・キャンセル・プロパティなど）」だけを配置したアクションバーが浮き上がります。
  - **※安全対策**: スマホUIからは複雑な「移動・コピー・回転」などの編集コマンドを排除し、閲覧・座標読取・簡単な作図に徹した安全設計にしています。図形選択時はプロパティの確認と選択解除のみが行えます。
* **フローティング座標ツールチップ**:
  - 1本指で画面をなぞると、指の付近に現在の測量X・Y座標値とスナップ状態がリアルタイムで表示されます（指を離しても表示を維持できる「XY常時表示」もヘッダー側で連動します）。
* **スナップ中心ズーム**:
  - ピンチ操作によるズームの際、端点や中点などをスナップ捕捉している場合は、その**捕捉しているポイントを中心にしてズーム**します（ズームスライダーは廃止され、ピンチ操作に統合されました）。

---

## 🛠 コマンドの使い方

新しい図形を描いたり、編集したりするには、画面左側のツールバーにあるアイコンをタップするか、画面下部のコマンドラインに文字を入力してコマンドを起動します。

### 📌 描画コマンド
| コマンド | エイリアス | 概要 |
| :--- | :--- | :--- |
| **LINE** | `L` | 線分を描画します。（1点目と2点目を指定） |
| **CIRCLE** | `C` | 円を描画します。（中心点と半径を指定） |
| **RECTANG** | `REC` | 長方形を描画します。（対角線を指定） |
| **ARC** | `A` | 円弧を描画します。（3点・中心などを指定） |
| **PLINE** | `PL` | 連続した線（ポリライン）を描画します。「完了」ボタンで確定します。 |
| **ELLIPSE** | `EL` | 楕円を描画します。（中心・長半径・短半径を指定） |
| **TEXT** | `T` | 文字を入力します。（プロパティから後で書き換え可能） |
| **HATCH** | `H` | 選択した閉じた図形を半透明で塗りつぶします。 |
| **POINT** | `PO` | 測量座標などで利用する「点」を描画します。 |

### ✏️ 編集コマンド
| コマンド | エイリアス | 概要 |
| :--- | :--- | :--- |
| **ERASE** | `E` | 図形を削除します。あらかじめ選択してから実行してもOKです。（長押し削除も可能） |
| **MOVE** | `M` | 図形を別の場所へ移動します。（基点と目的点を指定） |
| **COPY** | `CO` | 図形をコピーします。（基点と目的点を指定） |
| **OFFSET** | `O` | 任意の距離を指定して、図形を平行に複写します。 |
| **ROTATE** | `RO` | 図形を回転させます。（基点と参照角度を指定可能） |

### 📐 寸法・表示・機能
| コマンド | 概要 |
| :--- | :--- |
| **寸法 (DIM)** | 平行寸法、角度寸法、直径・半径寸法などを引き出します。 |
| **連続寸法 (DIMCONT)** | 選択した基準寸法から連続して寸法（直列・並列）を作図します。作図中のアクションバーで「⚙ 自動 (X/Y)」ボタンを押すと、寸法の向きを水平（X軸）または垂直（Y軸）に固定して引き出すことができます。 |
| **座標寸法 (DIMORDINATE)** | 「📍座標」ボタン。ワンタップで指定箇所のX/Y両方の測量座標を引出線付きで配置します。 |
| **UNDO/REDO** | 直前の操作を元に戻す、またはやり直します。 |
| **ZOOM (ZE)** | 🔍 全体。図面全体が画面に収まるよう表示位置を調整します。 |
| **PLAN** | 🧭 PLAN。設定したUCSの角度に合わせて、画面を自動回転させて「水平・直角（正面視点）」にします。 |

---

## 🗺 測量仕様に最適化された機能

本アプリは、測量図面や現場での利用に特化した設計となっています。

1. **X座標・Y座標の測量表示対応**
   * システム内の座標は「X: 測量座標系のX（北方向・数学的Y）」「Y: 測量座標系のY（東方向・数学的X）」として直感的に表示されます。
   * スマホ上の常時座標表示も小数点なし表示となり、現場のミリ単位読み取りに最適化されています。

2. **UCS（任意座標系）のフル活用**
   * **🎯 UCS**: 任意の場所に原点を移動し、そこからの相対的な座標（X,Y距離）を作成します。
   * **📈 UCS2点**: 画面上の2点を指定するだけで、その線を「測量X軸（進行方向）」とする新しい座標系を即座に作成します。現場の通り芯を基準にする際に便利です。（PC環境では、2点目を画面クリックした時点で即座に設定が完了します。また2点目指定時のガイド線は近接点等へのスナップに正確に追従します。）
   * **💾 保存**: 設定したUCSを名前をつけて保存し、いつでも切り替え・呼び出しが可能です。
   * **🧭 PLAN**: 斜めになった現場の通り芯（設定したUCS）を図面上に水平に表示し直すことで、現場の向きと画面の向きを一致させることができます。

---

## 📍 全画面座標読取モード（現場専用）

スマホヘッダーの「⛶ 全画面」または「🎯 座標読取」ボタンをタップすると、図面の閲覧・座標引出・測量作業に特化した「全画面座標読取モード」へ移行します。このモードでは通常のスマホ用ヘッダーやボトムバーが完全に隠れ、図面表示領域が最大化されます。

* **右側スライドツールバー（#fs-tools）**:
  - 全画面に入ると、デフォルトでは画面の右端に折りたたまれた状態（トグルボタン「◀」のみが見えている）になります。
  - 「◀」ボタンをタップすると、UCS設定や画層管理、図面保存・復元など、全画面で使用したいすべての機能を含むパネルが右側から滑らかにスライドイン（展開）します。
  - パネル左側の「▶」トグルボタンや、パネル内の機能ボタンをタップすると自動的に右端へ引っ込み、作業の邪魔になりません。
  - ボタン項目: `📍 座標` / `📐 連続寸法` / `🎯 UCS` / `📈 2点` / `🧭 PLAN` / `↩ WCS` / `👁️ 画層管理` / `🚫 タッチ消去` / `💾 保存` / `🗑️ 消去` / `💾 図面保存` / `📂 復元`
* **右下丸型コントロールボタン（#fs-controls）**:
  - 画面の右下に縦一列に配置される丸型のボタンで、片手親指で押しやすいよう直径44pxの大きさに最適化されています。
  - **OSNAP（⊕）**: オブジェクトスナップのON/OFF。ON時は枠線が緑色に変化。
  - **座標常時（XY）**: 座標表示を指を離しても維持する「常時表示モード」の切り替え。ON時は枠線が緑色に変化。
  - **全体表示（⛶）**: 図面全体を画面いっぱいに表示します。
  - **解除（✕）**: タップすると即座に全画面モードを解除し、通常のスマホ操作UI画面に戻ります。枠線は赤色。

---

## ⚙ その他の機能

* **OSNAP（オブジェクトスナップ）機能**
  * 画面下部の `OSNAP` ボタンでON/OFFを切り替えます。
  * `OSNAP`ボタンの横の「▼」を押すと設定パネルが開き、「端点」「中点」「交点」「中心」「近接点」「垂線」など、吸着させたい種類を細かく指定できます。ルーペ内にもスナップポイントが大きくハイライト（緑色）されます。
* **DXF / DWGファイルの読み込み・保存**
  * **読込**: 画面左上の「📁 開く」ボタンを押し、お手元の **.dxf** または **.dwg** ファイルを選択して読み込みます。
  * **保存**: 画面上部の「DXF」「DWG」ボタンを押すと、現在の状態をファイルとして書き出して保存（ダウンロード）できます。
* **画層一括管理（レイヤー管理）**
  - 画面上部の「👁️」ボタン（PC版）またはスマホ設定内の「画層一括管理を開く」をタップすると、フローティング形式の「画層一括管理」ダイアログが開きます。
  - **非表示画層をうっすら表示する**: チェックを入れると、非表示にされた画層の図形や寸法が不透明度15%の半透明で描画され、全体のレイアウトを確認しやすくなります。
  - **👇 タッチ非表示**: このボタンをオンにすると「タッチ非表示モード」となり、図面上のオブジェクトをタップするだけで、そのオブジェクトが属する画層が瞬時に非表示になります（カレント画層以外の非表示に便利です。Escキーで終了）。
  - **🔄 表示反転**: ワンタップで全画層の表示/非表示状態を反転します（現在の作図画層は表示が維持されます）。
* **図形選択とプロパティの編集**
  - 図形を選択すると、画面右側に「プロパティ」パネルが表示され、色、画層、座標、半径などの数値を直接編集できます。

---

## 🔄 更新履歴

最新の更新履歴については、[更新履歴.md](./更新履歴.md) をご参照ください。

**直近のアップデート**
* **2026年5月**: 図面の自動保存・読込機能の追加、iOS(Safari)での疑似フルスクリーン対応、寸法入力専用アクションバーの追加、ボタンタップ時の触覚フィードバックの追加など。
* **2026年4月**: 座標読取モード（全画面表示）の強化、UCS機能の大幅拡張、PLAN機能の追加、座標寸法(DIMORDINATE)の測量仕様への刷新など。
