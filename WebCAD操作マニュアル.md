# WebCAD 操作マニュアル

このドキュメントは、スマートデバイスやPCの両方で利用できるWebブラウザベースの2D CADアプリケーション **「Antigravity Web CAD」** の操作説明書です。

## 🌐 アクセスURL
お使いのスマートフォンやPCのブラウザ（Chrome, Safari等）から以下にアクセスしてください。特別なインストールは不要です。
**[https://antigravity-web-cad.vercel.app](https://antigravity-web-cad.vercel.app)**

---

## 📱 基本操作（画面の移動・拡大など）

### PCでの操作
* **画面の移動（パン）**: マウスの「ホイールボタン（中ボタン）」を押したままドラッグ。
* **画面の拡大・縮小（ズーム）**: マウスのホイールを前後に回転。
* **図形の選択**: 図形を左クリック（複数選択可）。何も選択していない状態でドラッグすると「範囲選択」になります。
  * **左から右へドラッグ（窓選択・青枠）**: 枠の中に完全に収まっている図形のみが選択されます。
  * **右から左へドラッグ（交差選択・緑枠）**: 枠に少しでも触れている図形がすべて選択されます。

### スマートフォン（タブレット）での操作
* **画面の移動（パン）**: 2本指で画面に触れ、そのままスライドします。
* **画面の拡大・縮小（ズーム）**: 2本指でつまんだり広げたりします（ピンチイン・ピンチアウト）。
* **図形の選択**: 図形付近を1回短くタップします。
* **正確な位置の指定（ルーペ機能）**: 画面を1本指で長押ししながらスライドすると、指の上に拡大鏡（ルーペ）が現れます。指を離した場所が決定ポイントになります。
* **範囲選択**: コマンド実行前（IDLE状態）や編集コマンドの選択待ちのときに1本指でスライドさせると、範囲選択の枠が描画されます。

---

## 🛠 コマンドの使い方

新しい図形を描いたり、編集したりするには、画面下部のコマンドラインに文字を入力するか、画面左側のツールバーにあるアイコンをタップしてコマンドを起動します。

### 📌 描画コマンド
| コマンド | エイリアス | 概要 |
| :--- | :--- | :--- |
| **LINE** | `L` | 線分を描画します。（1点目と2点目を指定） |
| **CIRCLE** | `C` | 円を描画します。（中心点と半径を指定） |
| **RECTANG** | `REC` | 長方形を描画します。（対角線を指定） |
| **ARC** | `A` | 円弧を描画します。（3点・中心などを指定） |
| **PLINE** | `PL` | 連続した線（ポリライン）を描画します。「完了」ボタンで確定します。 |
| **ELLIPSE** | `EL` | 楕円を描画します。（中心・長半径・短半径） |
| **TEXT** | `T` | 文字を入力します。（プロパティから後で書き換え可能） |
| **HATCH** | `H` | 選択した閉じた図形（長方形・円など）を半透明で塗りつぶします。 |
| **POINT** | `PO` | 測量座標などで利用する「点」を描画します。（後述のプロパティでサイズ変更などを指定可能） |

### ✏️ 編集コマンド
| コマンド | エイリアス | 概要 |
| :--- | :--- | :--- |
| **ERASE** | `E` | 図形を削除します。あらかじめ選択してから実行してもOKです。 |
| **MOVE** | `M` | 図形を別の場所へ移動します。（基点と目的点を指定） |
| **COPY** | `CO` | 図形をコピーします。（基点と目的点を指定） |
| **OFFSET** | `O` | 任意の距離を指定して、図形を平行に複写します。 |
| **ROTATE** | `RO` | 図形を回転させます。（基点と参照角度を指定可能） |

### 📐 寸法・表示・その他
| コマンド | エイリアス | 概要 |
| :--- | :--- | :--- |
| **DIM** | (DLI, DAL等) | さまざまな寸法記号（平行、角度、半径など）を引き出します。 |
| **UNDO** | `U` | 直前の操作を元に戻します。 |
| **REDO** | `REDO` | 戻した操作をやり直します。 |
| **ZOOM** | `ZE` | 図面全体が画面に収まるよう表示位置を調整します。 |
| **UCS** | - | ユーザー座標系。図面上の特定のポイントを「仮の原点 (X:0, Y:0)」に設定・移動させます。 |
| **WCS** | - | 絶対座標系。移動した原点 (UCS) を本来の位置 (WCS) にリセットします。 |

---

## 🗺 測量座標系に最適化された機能

本アプリは、測量図面や現場での利用に特化した設計となっています。

1. **X座標・Y座標の入れ替え表示**
   * 通常の数学的CADとは異なり、プロパティパネル上での座標欄は「X: 測量（画面の縦方向）」「Y: 測量（画面の横方向）」として入れ替え表示されています。
   * スマホ上の常時座標表示も `X: 0  Y: 0` の並びで小数点なし（0桁）表示となり、見やすくなっています。

2. **ポイントオブジェクトのサイズ連動**
   * スマホ画面上のピクセル固定ではなく、「図面上の寸法（10単位）」としてポイント（点）が固定されます。ズームレベルに応じて大きさが変化します。

3. **UCS（任意原点）での座標入力**
   * `UCS` コマンドで原点を移動させた後、オブジェクトを選択して右側のプロパティパネルで座標値を見ると、**移動した新しい原点からの距離**が表示されます。
   * プロパティから直接数値（XやYなど）を入力して図形の場所を調整することも可能です。

---

## ⚙ その他の機能

* **OSNAP（オブジェクトスナップ）機能**
  * 画面下部の `OSNAP` ボタンでON/OFFを切り替えます。
  * `OSNAP`ボタンを長押し（または右クリック）すると設定パネルが開き、「端点」「中点」「交点」「中心」「垂線」など、吸着させたい種類を細かく指定できます。
* **DXFファイル読み込み**
  * 画面左上の「読込」ボタンを押し、お手元のDXF形式の図面ファイルを選択するだけで読み込めます。PCの場合は画面へのドラッグ＆ドロップでも読み込み可能です。
  * スマホで開く場合は、あらかじめ本体やクラウド（iCloud, Googleドライブ等）にDXFを保存しておいてください。
* **画層（レイヤー）とプロパティの編集**
  * **画層パネル（左上）**: 読み込んだ図面のレイヤーごとに「表示/非表示（👁️アイコン）」を切り替えられます。
  * **プロパティパネル（右上）**: 図形を選択すると自動的には開かず、任意でパネルボタンを押した際に展開されます。選択図形の**サイズ変更**や**個別の表示ON/OFF**、**色の変更**が設定可能です。

※ご不明な点が発生した場合や図面がおかしくなった場合は、`ZE`（全体表示）や `U`（元に戻す）を使うと便利です。
